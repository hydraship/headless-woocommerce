steps:
  - name: docker-build-push
    image: woodpeckerci/plugin-docker-buildx
    # privileged: true
    settings:
      repo: registry.digitalocean.com/${CI_REPO_OWNER}/${CI_REPO_NAME}
      registry: registry.digitalocean.com
      tags: 
        - ${CI_COMMIT_SHA}
        - ${CI_COMMIT_TAG}
        - latest
      dry-run: false
      username:
        from_secret: docker_password      
      password:
        from_secret: docker_password
    when:
      - branch: hydrastore-ui
        event: push
      - event: tag
        ref: refs/tags/*-alpha
      - event: tag
        ref: refs/tags/*-beta
      - event: tag
        ref: refs/tags/*-rc
  - name: update-infra-repo-and-update-image-tag-dev
    image: gcr.io/cloudkite-public/yq-git:20231004
    environment:
      APPICLATIOM_HELM_CHART: hydrastore
      GIT_SSH_KEY:
        from_secret: GIT_SSH_KEY
    commands:
      - |
        mkdir -p /root/.ssh && \
        ssh-keyscan -t rsa github.com | tee github-key-temp | ssh-keygen -lf - && \
        cat github-key-temp >> /root/.ssh/known_hosts && \
        rm github-key-temp
        echo "$GIT_SSH_KEY" > /root/.ssh/id_ed25519
        chmod 400 /root/.ssh/id_ed25519
        git clone git@github.com:hydraship/infra.git
        cd infra
        git checkout main
        yq eval -i ".standard-app.hydrastore.tag=\"${CI_COMMIT_SHA}\"" "kubernetes/helm/apps/${APPICLATIOM_HELM_CHART}/dev.values.yaml"
        git config --global user.email "engineering@hydraship.com"
        git config --global user.name "WoodperckerCl[bot]"
        git add "kubernetes/helm/apps/${APPICLATIOM_HELM_CHART}/dev.values.yaml"
        git commit -m "update ${APPICLATIOM_HELM_CHART}'s image tag to ${CI_COMMIT_SHA} in dev.values.yaml"
        git fetch origin main
        git rebase origin/main
        git push origin main
    when:
      - branch: hydrastore-ui
        event: push
      - event: tag
        ref: refs/tags/[0-9]*.[0-9]*.[0-9]*-alpha
      - event: tag
        ref: refs/tags/[0-9]*.[0-9]*.[0-9]*-beta
      - event: tag
        ref: refs/tags/[0-9]*.[0-9]*.[0-9]*-rc
  - name: check-docker-image-existence
    image: digitalocean/doctl:latest
    environment:
      DIGITALOCEAN_ACCESS_TOKEN:
        from_secret: docker_password
    commands:
      - |
        apk update && apk add jq
        export REGISTRY="registry.digitalocean.com/hydraship"
        export IMAGE_NAME=${CI_REPO_NAME}:${CI_COMMIT_SHA}
        echo "Checking if image exists ${CI_REPO_NAME}:${CI_COMMIT_SHA}"
        EXISTS=$(/app/doctl registry repository list-manifests ${CI_REPO_NAME} --output json | jq --arg image_tag "${CI_COMMIT_SHA}" '[.[] | select(.tags[]? == $image_tag)] | length')
        if [ "$EXISTS" -eq "0" ]; then
          echo "Image does not exist: ${CI_REPO_NAME}:${CI_COMMIT_SHA}. Trigger dev pipeline first to build the image."
          exit 1
        else
          echo "Image exists: ${CI_REPO_NAME}:${CI_COMMIT_SHA}"
        fi
    when:
      - event: tag
        ref: refs/tags/[0-9]*.[0-9]*.[0-9]*
        evaluate: 'not (CI_COMMIT_TAG contains "rc" || CI_COMMIT_TAG contains "alpha" || CI_COMMIT_TAG contains "beta")'
  - name: update-infra-repo-and-update-image-tag-prod
    image: gcr.io/cloudkite-public/yq-git:20231004
    environment:
      GIT_SSH_KEY:
        from_secret: GIT_SSH_KEY
    commands:
      - |
        mkdir -p /root/.ssh && \
        ssh-keyscan -t rsa github.com | tee github-key-temp | ssh-keygen -lf - && \
        cat github-key-temp >> /root/.ssh/known_hosts && \
        rm github-key-temp
        echo "$GIT_SSH_KEY" > /root/.ssh/id_ed25519
        chmod 400 /root/.ssh/id_ed25519
        git clone git@github.com:hydraship/infra.git
        cd infra
        git checkout main
        yq eval -i ".standard-app.hydrastore.tag=\"${CI_COMMIT_SHA}\"" "kubernetes/helm/apps/${APPICLATIOM_HELM_CHART}/prod.values.yaml"
        git config --global user.email "engineering@hydraship.com"
        git config --global user.name "WoodperckerCl[bot]"
        git add "kubernetes/helm/apps/${APPICLATIOM_HELM_CHART}/prod.values.yaml"
        git commit -m "update ${APPICLATIOM_HELM_CHART}'s image tag to ${CI_COMMIT_SHA} in prod.values.yaml"
        git fetch origin main
        git rebase origin/main
        git push origin main
        if [ $? -eq 0 ]; then
          echo "Git push succeeded."
        else
          echo "Git push failed."
          exit 1
        fi
    when:
      - event: tag
        ref: refs/tags/[0-9]*.[0-9]*.[0-9]*
        evaluate: 'not (CI_COMMIT_TAG contains "rc" || CI_COMMIT_TAG contains "alpha" || CI_COMMIT_TAG contains "beta")'
      